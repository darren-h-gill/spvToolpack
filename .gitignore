node_modules
.DS_Store
.claude

# Ignore dist in general but track the built library files for CDN use via jsDelivr
# (glob form — trailing slash prevents exceptions from working)
dist/*
!dist/spv-toolpack.umd.js
!dist/spv-toolpack.es.js
!dist/spv-toolpack.md
